import styled from "styled-components";

const LogoText = styled.h1`
  font-family: "Saira Stencil One", cursive;
  font-size: 38px;
  letter-spacing: 1px;
  color: #47597E;
`;

export default function Logo() {
  return <LogoText>ToysCamp</LogoText>;
}
